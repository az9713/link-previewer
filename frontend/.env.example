# Frontend Environment Variables
# Copy this file to .env and fill in your values

# API URL - The backend server URL
# For local development (using Vite proxy), leave this commented out:
# VITE_API_URL=/api

# For production, set this to your Railway backend URL:
# VITE_API_URL=https://your-backend.up.railway.app

# Note: All Vite environment variables must start with VITE_
# These are embedded at BUILD TIME, not runtime
